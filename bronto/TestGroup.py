"""
TestGroup.py

Contains the TestGroup class, which should be inherited for all Bronto
test groups
"""

class TestGroup:
  """
  class TestGroup

  TestGroup is just an empty function that is inherited from in
  individual test groups.  This is used just as a marker so that
  Bronto knows what classes are test groups and what classes are just
  'normal' classes.
  """
